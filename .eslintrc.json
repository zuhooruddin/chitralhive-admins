{
  "extends": "next/core-web-vitals",
  "rules": {
    // You can disable specific rules here if needed
    // "@next/next/no-img-element": "off", // Already handled with eslint-disable comments
    // "react-hooks/exhaustive-deps": "warn" // Keep as warning (default)
  }
}
